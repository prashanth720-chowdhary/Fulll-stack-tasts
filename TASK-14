const express = require('express');
const app = express();

const tasks = [{ id: 1, userId: "1", title: "Complete TASK-13" }];

app.get('/tasks', (req, res) => {
    res.json(tasks);
});

app.listen(3002, () => console.log('Task Service running on port 3002'));
